import { Meta } from '@storybook/addon-docs';
import { ColorSwatchTable } from './ColorSwatchTable';

<Meta title="Concepts/Colors" />

# Colors
Below are the colors used in the app.

There are two types of colors: `default` and `foreground`.

`Default` colors are the base colors of the app.

`Foreground` are used for text and icons that are on top of the `default` colors.

<ColorSwatchTable />
