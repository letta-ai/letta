import { Meta } from '@storybook/addon-docs';
import { Button, Panel, PanelManager, PanelRenderArea } from '@letta-web/component-library';
import { PanelBar } from './PanelBar/PanelBar';

<Meta title="Concepts/Panelling System" />

# Panel
Panels are used to manage the layout of the Agent Development Environment. They can be moved, resized, and docked to different areas of the screen.

## Usage

<div className="flex flex-col gap-4">
  <PanelManager>
    <div className="gap-3 flex">
      <Panel id={['test']} trigger={<Button label="Activate Panel 1" />} title="Main panel 1">
        <PanelBar
          actions={(
            <Button variant="default" color="secondary" label="Button 1" />
          )}
        >
        </PanelBar>

      </Panel>

      <Panel id={['test', 'data']} trigger={<Button label="Activate The subpanel" />} title="Sub panel 1">
        <span className="text-content">Panel Content</span>

      </Panel>

      <Panel id={['different']} trigger={<Button label="Activate Another panel" />} title="Main panel 2">
        <span className="text-content">Panel Content</span>
      </Panel>
    </div>


    <PanelRenderArea />

  </PanelManager>
</div>

