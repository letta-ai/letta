import { Meta, Story } from '@storybook/addon-docs';
import { IconHelper } from './IconHelper';
import * as IconStory from './Icon.stories';
import { Controls, Source } from '@storybook/blocks';

<Meta title="Core/Icon" />

# Table of Contents

- [Icons](#icons)
- [Styling Icons](#styling-icons)
- [Creating a new icon](#creating-a-new-icon)

# Icons

Icons are used to represent actions, objects, and concepts in the app. Click on the icon to copy the component to your clipboard.

<IconHelper />

# Styling Icons

<div
  style={{ height: 300 }}
  className="border flex items-center justify-center p-6"
>
  <Story of={IconStory.Primary} />
</div>
<Controls of={IconStory.Primary} />
<Source of={IconStory.Primary} />

# Creating a new icon

To create a new icon, follow these steps:

1. Create a new SVG file in the `libs/component-library/src/lib/icons/sources` directory. The file should be named after the icon in a dash format, e.g., `my-icon.svg`.
2. Run `npm run cl:generate-icons` to generate the new icon component.

# Icon Sources
We get our icons from
https://fonts.google.com/icons?icon.query=folder&icon.size=24&icon.color=%235f6368

Export icons at 20x20px, make sure to use the "sharp" style to get hard corners (optical size = 20, grade = 0, weight = 400). Once you have the SVG, make sure to set the fill to currentColor (it may get set to a hardcoded value during the download).

Make sure you export fonts at size 16px
