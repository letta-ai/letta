import type { Tool } from '../../_autogenerated/requests';

export function isLettaTool(tool: Tool) {
  return [
    'letta_core',
    'letta_memory_core',
    'letta_multi_agent_core',
    'letta_sleeptime_core',
  ].includes(tool.tool_type || '');
}
