{
  "$schema": "https://json-schema.org/schema",
  "$id": "Setup",
  "title": "",
  "type": "object",
  "properties": {
    "DATABASE_URL": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the DATABASE_URL?"
    },
    "GOOGLE_CLIENT_ID": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What is the GOOGLE_CLIENT_ID?"
    },
    "GOOGLE_CLIENT_SECRET": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "x-prompt": "What is the GOOGLE_CLIENT_SECRET?"
    }
  },
  "required": ["DATABASE_URL"]
}
