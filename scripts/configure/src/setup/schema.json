{
  "$schema": "https://json-schema.org/schema",
  "$id": "Setup",
  "title": "",
  "type": "object",
  "properties": {
    "DATABASE_URL": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the DATABASE_URL? (leave blank for local-dev default)"
    },
    "GOOGLE_CLIENT_ID": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What is the GOOGLE_CLIENT_ID?"
    },
    "GOOGLE_CLIENT_SECRET": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "x-prompt": "What is the GOOGLE_CLIENT_SECRET?"
    },
    "GOOGLE_REDIRECT_URI": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 3
      },
      "x-prompt": "What is the GOOGLE_REDIRECT_URI?"
    },
    "REDIS_HOST": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 4
      },
      "x-prompt": "What is the REDIS_HOST URL? (leave blank for local-dev default)"
    },
    "REDIS_PORT": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 5
      },
      "x-prompt": "What is the REDIS_PORT? (leave blank for local-dev default)"
    },
    "REDIS_PASSWORD": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 6
      },
      "x-prompt": "What is the REDIS_PASSWORD? (leave blank for local-dev default)"
    },
    "LETTA_AGENTS_ENDPOINT": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 7
      },
      "x-prompt": "What is the LETTA_AGENTS_ENDPOINT? (leave blank for local-dev default)"
    },
    "NEXT_PUBLIC_CURRENT_HOST": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 8
      },
      "x-prompt": "What is the NEXT_PUBLIC_CURRENT_HOST? (leave blank for local-dev default)"
    },
    "MIXPANEL_TOKEN": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 9
      },
      "x-prompt": "What is your MIXPANEL_TOKEN? (very optional)"
    }
  },
  "required": ["DATABASE_URL"]
}
