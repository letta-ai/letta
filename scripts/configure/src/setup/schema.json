{
  "$schema": "https://json-schema.org/schema",
  "$id": "Setup",
  "title": "",
  "type": "object",
  "properties": {
    "DATABASE_URL": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the DATABASE_URL?"
    }
  },
  "required": ["DATABASE_URL"]
}
