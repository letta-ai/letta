import { GET as publicHandler } from '../../v1/[...ts-rest]/route';

export {
  publicHandler as GET,
  publicHandler as POST,
  publicHandler as PUT,
  publicHandler as PATCH,
  publicHandler as DELETE,
  publicHandler as OPTIONS,
};
