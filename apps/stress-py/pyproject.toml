[project]
name = "stress-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "letta-client>=0.1.323",
]

[dependency-groups]
dev = [
    "ipdb>=0.13.13",
]
