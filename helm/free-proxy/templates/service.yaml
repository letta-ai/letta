apiVersion: v1
kind: Service
metadata:
  name: {{ include "free-proxy.fullname" . }}
  labels:
    {{- include "free-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "free-proxy.selectorLabels" . | nindent 4 }}
