apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-api-voice.fullname" . }}
  labels:
    {{- include "cloud-api-voice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-api-voice.selectorLabels" . | nindent 4 }}
