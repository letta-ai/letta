# Default values for cloud-api-voice.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

project_id: &project_id memgpt-428419
lettaEnv: &lettaEnv prod
replicaCount: 1

image:
  repository: us-central1-docker.pkg.dev/memgpt-428419/letta/cloud-api-voice
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations:
    iam.gke.io/gcp-service-account: cloud-api-voice-prod-sa@memgpt-428419.iam.gserviceaccount.com
  # If not set and create is true, a name is generated using the fullname template
  name: "cloud-api-voice"

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: LoadBalancer
  port: 80

ingress:
  enabled: true
  className: "nginx"
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$1
  hosts:
    - host: voice.letta.com
      paths:
        - path: /(.*)
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

livenessProbe:
  httpGet:
    path: /
    port: 8080
readinessProbe:
  httpGet:
    path: /
    port: 8080

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 4
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

env:
  HOST: "0.0.0.0"
  PORT: 8080
  LETTA_AGENTS_ENDPOINT: "http://memgpt-server-voice:8083"
  REDIS_HOST: "10.167.199.148"
  LETTA_PG_DB: "memgpt"
  LETTA_PG_USER: "staff"
  LETTA_PG_HOST: "104.198.231.73"
  LETTA_PG_PORT: 6432
  CLICKHOUSE_ENDPOINT: "https://apyhgglpro.us-central1.gcp.clickhouse.cloud:8443"
  CLICKHOUSE_DATABASE: "otel"
  CLICKHOUSE_USERNAME: "default"
  # TEMPORAL_LETTUCE_API_HOST: "lettuce.tmhou.tmprl.cloud:7233"

secretsProvider:
  secrets:
    - OPENAI_API_KEY
    # - TEMPORAL_LETTUCE_CA_PEM
    # - TEMPORAL_LETTUCE_CA_KEY
    - STRIPE_WEBHOOK_SECRET
    - LAUNCH_DARKLY_SDK_KEY
    - STRIPE_SECRET_KEY
    - SENTRY_DSN
    - CLICKHOUSE_PASSWORD
    - LETTA_PG_PASSWORD
    - DATABASE_URL
