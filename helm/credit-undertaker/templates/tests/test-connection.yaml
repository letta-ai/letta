apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "credit-undertaker.fullname" . }}-test-connection"
  labels:
    {{- include "credit-undertaker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "credit-undertaker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
