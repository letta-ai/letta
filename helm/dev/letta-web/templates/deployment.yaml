apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "letta-web.fullname" . }}
  labels:
    {{- include "letta-web.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "letta-web.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "letta-web.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "letta-web.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          env:
            - name: NEXT_PUBLIC_CURRENT_HOST
              value: {{ .Values.env.LETTA_API_ENDPOINT | quote }}
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.secretsProvider.secrets }}
            {{- range .Values.secretsProvider.secrets }}
            {{/* this is the one place we must not use the k8s compatible variable name */}}
            - name: {{ . }}
              valueFrom:
                secretKeyRef:
                  name: {{ include "letta-web.k8sEnvServicePrefix" $ }}{{ . | replace "_" "-" | lower }}
                  key: {{ include "letta-web.k8sEnvServicePrefix" $ }}{{ . | replace "_" "-" | lower }}
            {{- end }}
            {{- end }}
          volumeMounts:
          {{- with .Values.volumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.secretsProvider.secrets }}
          {{- range .Values.secretsProvider.secrets }}
            - name: {{ include "letta-web.k8sEnvServicePrefix" $ }}{{ . | replace "_" "-" | lower }}
              mountPath: "/var/secrets/{{ . }}"
          {{- end }}
          {{- end }}
      volumes:
      {{- with .Values.volumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.secretsProvider.secrets }}
      {{- range .Values.secretsProvider.secrets }}
        - name: {{ include "letta-web.k8sEnvServicePrefix" $ }}{{ . | replace "_" "-" | lower }}
          csi:
            driver: secrets-store-gke.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ include "letta-web.k8sEnvServicePrefix" $ }}secrets
      {{- end }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
