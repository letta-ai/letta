apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "model-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "model-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "model-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
