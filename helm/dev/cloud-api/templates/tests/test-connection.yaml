apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-api.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
