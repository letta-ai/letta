apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "letta-web.fullname" . }}-test-connection"
  labels:
    {{- include "letta-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "letta-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
